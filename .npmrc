# Build scripts are allowlisted via `pnpm.onlyBuiltDependencies` in package.json,
# which is what pnpm 9 reads. `only-built-dependencies` here is a pnpm 10 setting
# and is ignored by the pinned pnpm version.
ignore-scripts=false
